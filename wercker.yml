box: wercker/python
build:
    steps:
        - script:
            name: virtualenv
            code: |
                virtualenv as.venv
        - script:
            name: pip
            code: |
                ./as.venv/bin/pip install tox
        - script:
            name: tox test
            code: |
                ./as.venv/bin/tox

