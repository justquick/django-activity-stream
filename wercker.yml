box: wercker/python
build:
    steps:
        - virtualenv:
            name: setup virtualenv
            install_wheel: false

        - pip-install
            packages_list: "tox==1.7.1"

        - script:
            name: tox
            code: |
                tox

